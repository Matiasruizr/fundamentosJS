var edad = 10

edad += 2 // La edad aumenta en un numero x

edad++ // Incrementa en uno la edad

var peso = 80
peso -= 2 // Peso aumenta en un numero x
peso--

var sandwich = 1
peso += sandwich // Aumenta peso en el valor de sandwich

var correr = 2
peso -= correr


// Decimales
var precioVino = 200.3
var total = Math.round(precioVino * 100 * 3 ) / 100
var totalStr = total.toFixed(2) // Numero de decimales
var total2 = parseFloat(totalstr)

var pizza = 8
var personas = 2
var cantidadPizzaPersona = pizza / personas